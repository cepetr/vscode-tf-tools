# IntelliSense manifest fixture: valid structure but the expected artifacts
# will not exist on disk. Used to test the "missing" compile-commands state
# and stale-state clearing behavior.

models:
  - id: T2T1
    name: Trezor Model T (v1)
    artifactFolder: model-t
  - id: T3W1
    name: Trezor Model T3 (w1)
    artifactFolder: model-t3

targets:
  - id: hw
    name: Hardware
    shortName: HW

components:
  - id: core
    name: Core
    artifactName: compile_commands_core
  - id: prodtest
    name: Prodtest
    artifactName: compile_commands_prodtest
