{
  // Intentionally malformed JSONC fixture for invocation-time parse failure coverage.
  "type": "gdb",
  "request": "launch",
  "name": "Malformed ${tfTools.model}",
  "program": "${tfTools.executablePath}",
  "cwd": "${workspaceFolder}",
  "environment": [
    {
      "name": "TARGET",
      "value": "${tfTools.target}"
    }
  ],
  "stopAtEntry": false
