# Flash/Upload actions manifest: invalid flashWhen/uploadWhen expressions.
# Used to verify that invalid action rules surface as validation issues.

models:
  - id: T2T1
    name: Trezor Model T (v1)
    artifactFolder: model-t

targets:
  - id: hw
    name: Hardware

components:
  - id: core
    name: Core
    artifactName: compile_commands_core
    flashWhen: "model(UNKNOWN_MODEL)"
    uploadWhen: "not a valid expression"
  - id: prodtest
    name: Prodtest
    artifactName: compile_commands_prodtest
    flashWhen: 42
